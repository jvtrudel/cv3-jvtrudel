
if [ -d "datawiz" ]; then
   cd datawiz
   git pull
   cd ..
else
   git clone https://github.com/datalyse/datawiz.git
fi


if [ -d "cv30" ]; then
   cd cv30
   git pull
   cd ..
   ./cv30/scripts/build.site
else
   git clone https://github.com/datalyse/cv30.git
fi

 ./cv30/scripts/build.site
